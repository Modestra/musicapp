import { TableContainer, Table, TableHead } from "@mui/material";

export default function AppTable() {
  return (
    <TableContainer>
      <Table>
        <TableHead></TableHead>
      </Table>
    </TableContainer>
  );
}
